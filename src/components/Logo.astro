<img src="https://straight-code.github.io/assets/straight-code.png" id="img" />

<style lang="scss">
	.logo {
		margin: 2rem auto;
	}

	.title {
		font-family: var(--font-sans);
		font-size: 1rem;
	}
	.title img {
		margin-right: -100%;
	}
	.title img text {
		font-size: 16px;
		font-family: var(--font-sans);
	}
	.title img text.span {
		fill: white;
		font-size: 16.2px;
		transform: translate(0, 18px);
	}
	.title img text.em {
		fill: var(--color-green);
		transform: translate(0, 36px);
	}

	@media (min-width: 40em) {
		.title img {
			margin-right: 0;
			margin-bottom: -40px;
		}
		.title img text.span {
			font-size: 16px;
		}
		.title img text.em {
			transform: translate(190px, 18px);
		}
	}
</style>
